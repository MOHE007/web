# 变更日志

## 2025-11-11
- 新增：Google News 导入支持分类过滤（`include`/`exclude`），默认排除娱乐、体育。
- 新增：导入阶段可选自动打分（`score=true`），支持七因子显著性评分。
- 优化：显著性分数权重固定，`positivity` 仅占 1/20，其余六因子均分 95%。
- 回退：未配置 DeepSeek 密钥时使用本地启发式打分，保证端到端可用性。
- 文档：更新 Render 部署清单，明确环境变量与服务端口约定。